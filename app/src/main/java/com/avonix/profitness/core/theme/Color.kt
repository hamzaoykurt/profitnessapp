package com.avonix.profitness.core.theme

import androidx.compose.ui.graphics.Color

// ╔══════════════════════════════════════════════════════════════════╗
// ║        PROFITNESS — Design System v3.0 "NEON FORGE"             ║
// ║  Inspired by: Premium fitness app reference images               ║
// ║  Core: Dark base + Electric Lime accent + Amber warmth           ║
// ╚══════════════════════════════════════════════════════════════════╝

// ── Base Surfaces ────────────────────────────────────────────────────────────
val Surface0      = Color(0xFF0A0A0F)   // Ekran zemini — near-black with warmth
val Surface1      = Color(0xFF111117)   // Kart yüzeyi
val Surface2      = Color(0xFF18181F)   // Elevated kart
val Surface3      = Color(0xFF21212A)   // En yüksek elevation
val SurfaceStroke = Color(0xFF2A2A35)   // Kart border

// ── Electric Lime — Primary Accent ───────────────────────────────────────────
val Lime          = Color(0xFFCBFF4D)   // Ana lime — progress, butonlar
val LimeBright    = Color(0xFFDDFF77)   // Parlak varyant
val LimeDim       = Color(0xFF8AB830)   // Karanlık varyant
val LimeGlow      = Color(0x40CBFF4D)   // Glow için
val LimeText      = Color(0xFF0A0A0F)   // Lime üstündeki yazı (dark bg)

// ── Amber — Secondary Warmth ─────────────────────────────────────────────────
val Amber         = Color(0xFFFFA726)   // Amber accent
val AmberWarm     = Color(0xFFFFBF59)   // Sıcak amber
val AmberDark     = Color(0xFFCC7A00)   // Koyu amber
val AmberGlow     = Color(0x40FFA726)   // Bloom için

// ── Vibrant Card Category Colors (Ref. Image 1) ───────────────────────────────
val CardCyan      = Color(0xFF00E5D3)   // Teal/Cyan kategori
val CardCoral     = Color(0xFFFF6B6B)   // Coral kategori
val CardPurple    = Color(0xFF9B59FF)   // Purple kategori
val CardGreen     = Color(0xFF32FF7E)   // Green kategori

// ── Text Hierarchy ────────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFF8F8F8)   // Ana metin
val TextSecondary = Color(0xFF9A9AB0)   // İkincil metin
val TextMuted     = Color(0xFF5A5A72)   // Soluk metin

// ── Legacy Aliases ────────────────────────────────────────────────────────────
val Abyss        = Surface0
val Depth1       = Surface1
val Depth2       = Surface2
val Depth3       = Surface3
val Snow         = TextPrimary
val Mist         = TextSecondary
val Fog          = TextMuted
val ObsidianBase = Surface0
val Obsidian1    = Surface1
val Obsidian2    = Surface2
val Obsidian3    = Surface3
val ObsidianText = TextPrimary
val ObsidianSub  = TextSecondary
val ObsidianMuted= TextMuted
val AmberCore    = Amber
val AmberRimLight= Color(0x50FFA726)
val AmberGlow15  = Color(0x26FFA726)
val AmberGlow08  = Color(0x14FFA726)
val AmberGlow40  = AmberGlow
val ObsidianStroke = SurfaceStroke
val Forge300     = Color(0xFFFFBB33)
val Forge400     = AmberWarm
val Forge500     = Amber
val Forge600     = AmberDark
val Forge700     = Color(0xFF994000)
val Ember500     = Color(0xFFEF4444)
val Ember400     = Color(0xFFF87171)
val Lava500      = Color(0xFFF97316)
val LimeAccent   = Lime
val CriticalRed  = Color(0xFFEF4444)
val Red400       = CriticalRed
val RimLightTop  = Color(0x40FFFFFF)
val ShadowDeep   = Color(0xCC000000)
val Void         = Color(0xFF030303)
val Amber400     = AmberWarm
val Amber500     = Amber
val Orange500    = Lava500
val WhiteGlow    = Color(0x1AFFFFFF)
val GlassBorder      = Color(0x1AFFFFFF)
val BorderSoft   = Surface3
